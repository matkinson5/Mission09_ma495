
@{
}

@model BooksViewModel

<h1>Bookstore Project</h1>

@foreach (Book b in Model.Books)
{
<div class="card card-outline-primary m-1 p-1"> <!--show the different books and the information available-->
    <div class="bg-faded p-1">
        <h4>
            @b.Title
            <span class="badge bg-secondary" style="float:right">
                <small>@b.Price.ToString("c")</small>
            </span>
        </h4>
    </div>
    <div class="card-text p-1">
        <ul>
            <li>Author: @b.Author</li>
            <li>Publisher: @b.Publisher</li>
            <li>ISBN: @b.Isbn</li>
            <li>Classification: @b.Classification</li>
            <li>Category: @b.Category</li>
            <li>PageCount: @b.PageCount</li>
            <li>Price: @b.Price</li>
        </ul>
    </div>
</div>

}

<div page-booklist="@Model.PageInfo" page-action="Index" <!--The pages tab at the bottom of the page-->
     page-classes-enabled="true"
     page-class="btn" page-class-normal="btn-outline-dark"
     page-class-selected="btn-primary" class="btn-group pull-right m-1">
</div>



